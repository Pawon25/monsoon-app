/**
 * Deployment config. Change this one line when deploying the backend to
 * Render — app.js reads window.API_BASE_URL and falls back to localhost.
 */
window.API_BASE_URL = "http://localhost:8000";
// Example production value:
// window.API_BASE_URL = "https://varsha-sahayak-api.onrender.com";
